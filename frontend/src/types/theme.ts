// src/types/theme.ts

// Defines the palette options that the user can customize.
// All properties are optional.
export interface UserPalettePreferences {
  primaryMain?: string;
  secondaryMain?: string;
  backgroundDefault?: string;
  backgroundPaper?: string;
  textPrimary?: string;
  textSecondary?: string;
}